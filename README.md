# Python Calculator

Minimal implementation of arbitrary script to demonstrate SWE core practices: Git workflow, TDD with pytest, linting with flake8/black, and CI/CD via GitHub Actions

## Setup
1. Clone the repo
2. Create a virtual environment: `python -m venv .venv`
3. Activate it and run: `pip install -r requirements.txt`